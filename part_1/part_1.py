class CardGame:
    # constructor missing here for the class of CardGame, eg 
    # def __init__(self, name, cards):
        # self.name = name
        # self.cards = cards


  def check_for_ace(self, card): 
    if card.value = 1: # if card.value == 1:
      return True
    else # else:
      return False
   

  dif highest_card(self, card1 card2): #def highest_card(self, card1, card2):
  if card1.value > card2.value: #indent line 12, 13, 14 and 15
    return card #return card1
  else:
    return card2
        

# indent this entire block of code 
def cards_total(self, cards):
  total # total = 0
  for card in cards:
    total += card.value
    return "You have a total of" + total #return "You have a total of " + str(total) .......AND indent this line to the left so it has the same starting point as the FOR statement
